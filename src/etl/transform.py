from utils.logger import init_logger
logger = init_logger(__name__)


def transform (extraction):
    # This is a placeholder function for the transformation step
    # It simply returns the extraction data as is
    logger.info('Transformed data:')
    return extraction
    