from utils.logger import init_logger
logger = init_logger(__name__)

def load(transformation):
    # This is a placeholder function for the load step
    # It simply logs the data to the console
    logger.info('Loaded data:')